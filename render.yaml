services:
  - type: web
    name: m3u8-extractor
    env: node
    plan: free
    buildCommand: "yarn install"
    startCommand: "yarn start"
    envVars:
      - key: NODE_ENV
        value: production
    autoDeploy: true
